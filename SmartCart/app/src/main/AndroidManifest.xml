<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartCart"
        tools:targetApi="31">
        <activity
            android:name=".ui.theme.SaveAddressActivity"
            android:exported="false" />
        <activity
            android:name=".ui.theme.OrdersActivity"
            android:exported="false" />
        <activity
            android:name=".ui.theme.AccountDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.theme.AddressActivity"
            android:exported="false" />
        <activity
            android:name=".ui.theme.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".ui.theme.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.theme.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.theme.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmartCart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>